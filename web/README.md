# StudyLounge Web

Vite + React web console for StudyLounge.

## Scripts

- `npm run dev` starts the local web app.
- `npm run build` typechecks and builds production assets.
- `npm run lint` runs ESLint.

## API

Set `VITE_API_BASE_URL` when the backend is not running on `http://localhost:3000`.
